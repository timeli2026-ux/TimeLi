---
phase: 06-calendar-ui
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/calendar/recalibrate-dialog.tsx
  - src/app/(protected)/calendar/page.tsx
  - src/components/calendar/event-popover.tsx
  - src/components/calendar/dashboard-sidebar.tsx
autonomous: true
---

<objective>
Complete calendar interactions and polish, focusing on enhanced recalibrate flow with feedback prompt and dashboard stats updates.

Purpose: Finish Phase 6 by adding user feedback collection before schedule regeneration, enabling live dashboard updates after completion logging, and removing/disabling unused UI elements.
Output: Fully functional calendar UI with all interactions complete and ready for Phase 6.5 (Chat).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CALENDAR_REVISION.md
@.planning/phases/06-calendar-ui/06-03-SUMMARY.md
@.planning/phases/06-calendar-ui/06-04-SUMMARY.md
@.planning/phases/06-calendar-ui/06-05-SUMMARY.md

Source files:
@src/components/calendar/recalibrate-dialog.tsx
@src/components/calendar/event-popover.tsx
@src/app/(protected)/calendar/page.tsx
@src/components/calendar/dashboard-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feedback prompt to Recalibrate dialog</name>
  <files>src/components/calendar/recalibrate-dialog.tsx, src/app/(protected)/calendar/page.tsx, src/app/api/schedule/generate/route.ts</files>
  <action>
Enhance the RecalibrateDialog to include an optional feedback textarea where users can describe what they'd like to change before regenerating.

In recalibrate-dialog.tsx:
1. Add a Textarea for feedback between scope selection and warning
2. Add state for feedback text
3. Update RecalibrateDialogProps to include feedback in onConfirm callback: `onConfirm: (scope: RecalibrateScope, feedback?: string) => void`
4. Pass feedback to onConfirm when submitting
5. Add helpful placeholder text: "What would you like to change? (e.g., 'Move gym to evenings', 'More time for studying')"

In calendar/page.tsx:
1. Update handleRecalibrate to accept optional feedback parameter
2. Pass feedback to the /api/schedule/generate call in request body

In api/schedule/generate/route.ts:
1. Accept optional `feedback` field in request body
2. Log feedback for now (actual LLM integration in Phase 6.5)
3. No behavior change yet - just capture and log

Do NOT implement LLM parsing of feedback - that's Phase 6.5. Just capture and store for now.
  </action>
  <verify>
1. Open calendar page with existing schedule
2. Click Recalibrate button
3. Verify textarea appears with placeholder text
4. Enter feedback text, select scope, confirm
5. Check server logs for feedback being received
  </verify>
  <done>Recalibrate dialog has optional feedback textarea, feedback is passed to API</done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard after completion logging</name>
  <files>src/app/(protected)/calendar/page.tsx, src/components/calendar/dashboard-sidebar.tsx</files>
  <action>
Currently, when a user marks an event complete, the dashboard stats don't update. Fix this by:

In calendar/page.tsx:
1. After successful completion logging in handleCompletion, update the events state to mark the event as completed
2. Add a `completedStatus` field to ScheduleEventWithFlexibility (or track separately)
3. Update the event in state after completion so dashboard reflects the change

In dashboard-sidebar.tsx:
1. Add visual indicator for completed goals in GoalProgress section
2. Show completed vs scheduled ratio if tracking completions
3. Update utilization calculation to account for completed events

Alternative simpler approach: Just trigger a schedule reload after completion to get fresh data from API.
  </action>
  <verify>
1. Log a completion for an event
2. Verify dashboard updates without page refresh (or with minimal delay if using reload approach)
3. Goal progress should reflect the completion
  </verify>
  <done>Dashboard stats update after completion logging</done>
</task>

<task type="auto">
  <name>Task 3: Clean up event popover actions</name>
  <files>src/components/calendar/event-popover.tsx</files>
  <action>
The "Reschedule" button in the event popover is currently disabled. Since drag/drop already handles rescheduling, and LLM-based rescheduling comes in Phase 6.5:

1. Remove the disabled "Reschedule" button from the popover for now
2. Keep only "Mark Complete" as the action
3. Add a comment noting that "Reschedule via chat" will be added in Phase 6.5

This keeps the UI clean and focused on what's actually functional.
  </action>
  <verify>
1. Click on a goal event in the calendar
2. Verify popover shows only "Mark Complete" button
3. Verify button works (opens completion modal)
  </verify>
  <done>Event popover actions are clean with only functional buttons</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Recalibrate dialog shows feedback textarea
- [ ] Feedback is captured and logged by API
- [ ] Dashboard updates after completion logging
- [ ] Event popover actions are clean and functional
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Calendar interactions are polished and ready for Phase 6.5
- No unused/disabled UI elements left over
</success_criteria>

<output>
After completion, create `.planning/phases/06-calendar-ui/06-06-SUMMARY.md`
</output>
