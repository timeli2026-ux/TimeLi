---
phase: 06-calendar-ui
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/components/calendar/calendar-event.tsx, src/components/calendar/event-popover.tsx, src/components/calendar/week-grid.tsx, src/components/ui/popover.tsx]
autonomous: true
---

<objective>
Implement event display with distinct styling for locked vs AI-generated events, including rationale badges and flexibility indicators.

Purpose: Visually distinguish between fixed commitments (locked) and AI-scheduled goals, with explanations for why events are placed where they are.
Output: CalendarEvent component with proper styling, popover with rationale, and integration into WeekGrid.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-calendar-ui/06-01-SUMMARY.md

# Types from scheduling engine
@src/lib/scheduling/types.ts

# WeekGrid component to integrate with
@src/components/calendar/week-grid.tsx

# Existing UI components
@src/components/ui/button.tsx
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Popover component and create CalendarEvent</name>
  <files>src/components/ui/popover.tsx, src/components/calendar/calendar-event.tsx</files>
  <action>
First, add shadcn popover component:
```bash
npx shadcn@latest add popover --yes
```

Create CalendarEvent component:

Props:
- event: ScheduleEventWithFlexibility (from scheduling types)
- onClick?: () => void

Visual requirements by event type:

**Locked events (type: fixed, meal, sleep, commute):**
- Background: bg-muted (gray)
- Lock icon (lucide-react Lock) in top-right corner
- No flexibility indicator
- Not draggable (cursor: default)
- Subtle border

**AI-generated goal events (type: goal):**
- Background: Color based on realmId (use a simple color mapping, 6-8 colors)
- Flexibility dot in top-right:
  - High: green dot
  - Medium: yellow dot
  - Low: red dot
- Rationale badge at bottom if event.rationale exists
  - Show primary reason truncated to fit
  - "Why?" icon hint for expand
- Draggable cursor (cursor: grab when hoverable)

General styling:
- Rounded corners (rounded-md)
- Event title in text-xs font-medium, truncated if too long
- Time range in text-xs text-muted-foreground
- Height calculated from slot duration (15px per 15 minutes)
- Absolute positioning within the day column grid cell
- Z-index management: higher z-index for overlapping events

Create a color map utility:
```typescript
const REALM_COLORS: Record<string, string> = {
  default: 'bg-blue-100 border-blue-300 text-blue-900',
  // Add 6-8 realm colors
}
function getRealmColor(realmId?: string): string
```
  </action>
  <verify>npm run build succeeds</verify>
  <done>CalendarEvent component renders with correct styling for locked and goal events</done>
</task>

<task type="auto">
  <name>Task 2: Create EventPopover with rationale display</name>
  <files>src/components/calendar/event-popover.tsx</files>
  <action>
Create EventPopover component that shows on click:

Props:
- event: ScheduleEventWithFlexibility
- open: boolean
- onOpenChange: (open: boolean) => void
- children: React.ReactNode (trigger element)

Popover content structure:
1. **Header**
   - Event title (bold)
   - Time range (e.g., "9:00 AM - 10:30 AM")
   - Day of week

2. **Event Type Badge**
   - For locked: "Fixed" badge with lock icon
   - For goals: Realm/category name

3. **Rationale Section** (for goal events only)
   - "Why this time?" subheading
   - Primary rationale in regular text
   - Secondary rationale in text-muted-foreground (if exists)
   - Scoring factors as subtle tags/chips

4. **Flexibility Section** (for goal events only)
   - Flexibility level badge (High/Medium/Low)
   - Explanation text (e.g., "6 alternative slots available")

5. **Actions** (placeholder for Plan 06-03)
   - "Mark Complete" button (disabled, placeholder)
   - For goal events: "Reschedule" button (disabled, placeholder)

Styling:
- Use PopoverContent with appropriate padding
- Max width ~300px
- Sections separated with subtle borders
  </action>
  <verify>npm run build succeeds</verify>
  <done>EventPopover displays event details and rationale on click</done>
</task>

<task type="auto">
  <name>Task 3: Integrate events into WeekGrid with positioning</name>
  <files>src/components/calendar/week-grid.tsx, src/app/(protected)/calendar/page.tsx</files>
  <action>
Update WeekGrid to render events:

1. **Event positioning logic:**
   - Calculate top offset: (minutes from day start) / 15 * slot_height
   - Calculate height: duration / 15 * slot_height
   - Each day column is position: relative
   - Events are position: absolute within their day column

2. **Event grouping:**
   - Group events by dayOfWeek
   - Sort by start time within each day
   - Handle overlapping events (for now, just stack with slight offset)

3. **Rendering:**
   - For each day column, render events array
   - Wrap each CalendarEvent with EventPopover
   - Track open popover state (only one open at a time)

Update calendar page to show mock data:
- Create mock schedule data for testing (3-4 locked events, 4-5 goal events)
- Include various flexibility levels and rationale
- Render with the updated WeekGrid

Add loading and empty states:
- Loading: Skeleton grid with pulsing placeholders
- Empty: "No events scheduled" message with call to action
  </action>
  <verify>
npm run build succeeds
Calendar page shows events positioned correctly
Click on event shows popover with details
  </verify>
  <done>Events display on calendar with correct positioning, styling, and popover interaction</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] Locked events appear gray with lock icon
- [ ] Goal events appear colored by realm
- [ ] Flexibility dots visible on goal events
- [ ] Clicking event opens popover
- [ ] Popover shows rationale for goal events
- [ ] Events positioned correctly based on time
- [ ] Loading skeleton displays during fetch
</verification>

<success_criteria>

- All tasks completed
- Event display distinguishes locked vs AI-generated
- Rationale and flexibility visible to user
- No TypeScript errors
- Ready for drag/drop in Plan 06-03
  </success_criteria>

<output>
After completion, create `.planning/phases/06-calendar-ui/06-02-SUMMARY.md`
</output>
