---
phase: 06-calendar-ui
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified: [src/components/calendar/dashboard-sidebar.tsx, src/components/calendar/realm-pie-chart.tsx, src/components/calendar/goal-progress.tsx, src/app/(protected)/calendar/page.tsx, src/components/ui/progress.tsx, src/components/ui/separator.tsx]
autonomous: false
---

<objective>
Add left sidebar with realm statistics, pie chart, and goal progress tracking.

Purpose: Provide visual overview of schedule distribution and goal completion.
Output: Three-column calendar layout with dashboard on left and chatbox placeholder on right.
</objective>

<context>
Part of CALENDAR_REVISION.md plan. Dashboard shows:
- Quick stats (goal hours, goals count, deep work hours, utilization)
- Realm time distribution pie chart
- Per-goal progress bars
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts</name>
  <action>
npm install recharts
  </action>
</task>

<task type="auto">
  <name>Task 2: Create RealmPieChart component</name>
  <files>src/components/calendar/realm-pie-chart.tsx</files>
  <action>
- Use recharts PieChart with donut style
- Calculate time per realm from events
- Color-coded by realm (match calendar-event colors)
- Interactive tooltip showing hours and percentage
- Legend on right side
  </action>
</task>

<task type="auto">
  <name>Task 3: Create GoalProgress component</name>
  <files>src/components/calendar/goal-progress.tsx</files>
  <action>
- List top 5 goals with progress bars
- Show scheduled hours per goal
- Color-coded by realm
- "+N more goals" if more than 5
  </action>
</task>

<task type="auto">
  <name>Task 4: Create DashboardSidebar component</name>
  <files>src/components/calendar/dashboard-sidebar.tsx</files>
  <action>
- Quick stats grid (2x2):
  - Goal hours
  - Goals count
  - Deep work hours
  - Utilization percentage
- RealmPieChart section
- GoalProgress section
- Responsive (hidden on smaller screens)
  </action>
</task>

<task type="auto">
  <name>Task 5: Integrate into calendar page</name>
  <files>src/app/(protected)/calendar/page.tsx</files>
  <action>
- Three-column layout:
  - Left: DashboardSidebar (hidden on < lg)
  - Center: WeekGrid (main content)
  - Right: Chatbox placeholder (hidden on < xl)
- Pass events and stats to DashboardSidebar
- Add schedule stats state
  </action>
</task>

</tasks>

<verification>
- [ ] Pie chart displays realm distribution
- [ ] Progress bars show per-goal hours
- [ ] Stats grid shows accurate numbers
- [ ] Responsive layout works
- [ ] npm run build succeeds
</verification>
