---
phase: 06-calendar-ui
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified: [supabase/migrations/00006_schedule_persistence.sql, src/lib/scheduling/engine.ts, src/app/api/schedule/[weekStart]/route.ts, src/app/api/schedule/generate/route.ts, src/app/api/schedule/complete/route.ts, src/app/api/schedule/update/route.ts, src/app/(protected)/calendar/page.tsx, src/components/calendar/calendar-event.tsx]
autonomous: false
---

<objective>
Connect calendar to real scheduling API, persist generated schedules to database, and fix critical bugs identified in 06-03 verification.

Purpose: Replace mock data with real API integration and enable schedule persistence across sessions.
Output: Calendar that loads/saves real schedules from database with proper rationale display.
</objective>

<context>
Created as part of CALENDAR_REVISION.md to address issues found during 06-03 verification.

Key issues to fix:
1. Calendar uses mock data instead of real API
2. No schedule persistence (lost on refresh)
3. Rationale never attached to events (scheduler bug)
4. Locked events all gray (should be colored by realm)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for schedule persistence</name>
  <files>supabase/migrations/00006_schedule_persistence.sql</files>
  <action>
Create new tables:
- generated_schedules: Store weekly schedules (user_id, week_start, events jsonb)
- schedule_completions: Track completed/skipped events
- schedule_feedback: User preferences from chat
- schedule_conversations: Chat history for LLM

Add RLS policies, indexes, and triggers.
  </action>
</task>

<task type="auto">
  <name>Task 2: Fix scheduler rationale bug</name>
  <files>src/lib/scheduling/engine.ts</files>
  <action>
Import generateRationale and call it when creating events.
Attach rationale to each goal event based on scoring factors.
  </action>
</task>

<task type="auto">
  <name>Task 3: Create schedule CRUD API endpoints</name>
  <files>src/app/api/schedule/[weekStart]/route.ts, src/app/api/schedule/generate/route.ts, src/app/api/schedule/complete/route.ts, src/app/api/schedule/update/route.ts</files>
  <action>
- GET /api/schedule/[weekStart]: Load saved schedule
- POST /api/schedule/generate: Generate and persist new schedule
- PATCH /api/schedule/[weekStart]: Update single event
- POST /api/schedule/complete: Log completion with persistence
- POST /api/schedule/update: Move event with persistence
  </action>
</task>

<task type="auto">
  <name>Task 4: Update calendar page to use real API</name>
  <files>src/app/(protected)/calendar/page.tsx</files>
  <action>
- Remove all mock data (generateMockEvents)
- Fetch schedule from API on mount and week change
- Handle loading, error, and empty states
- Add "Generate Schedule" button for empty weeks
- Persist changes on drag/drop and completion
  </action>
</task>

<task type="auto">
  <name>Task 5: Fix locked event coloring</name>
  <files>src/components/calendar/calendar-event.tsx</files>
  <action>
- Add EVENT_TYPE_REALM_MAP for default realm colors
- Meals → health realm color
- Commute → personal realm color
- Fixed commitments → career realm color
- Only sleep remains gray
  </action>
</task>

</tasks>

<verification>
- [ ] Database migration creates all tables with RLS
- [ ] Scheduler attaches rationale to events
- [ ] Calendar loads real schedule from API
- [ ] Schedule persists across page refreshes
- [ ] Locked events colored by realm (not gray)
- [ ] npm run build succeeds
</verification>
