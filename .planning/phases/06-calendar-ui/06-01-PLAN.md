---
phase: 06-calendar-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/calendar/week-grid.tsx, src/components/calendar/time-column.tsx, src/app/(protected)/calendar/page.tsx, src/app/(protected)/calendar/layout.tsx, src/lib/calendar-utils.ts]
autonomous: true
---

<objective>
Build the week view calendar grid with 7 columns (Mon-Sun), 15-minute time slots, and week navigation.

Purpose: Create the foundational calendar UI structure that will display scheduled events from Phase 5's scheduling engine.
Output: Working week grid component with time column, day headers, and week navigation (current to +4 weeks).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scheduling-engine/05-03-SUMMARY.md

# Scheduling types needed for calendar
@src/lib/scheduling/types.ts

# Existing UI components
@src/components/ui/button.tsx
@src/components/ui/card.tsx

# Protected layout structure
@src/app/(protected)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create calendar utility functions and types</name>
  <files>src/lib/calendar-utils.ts</files>
  <action>
Create utility functions for calendar operations:
- `getWeekStart(date: Date): Date` - returns Monday of the week
- `getWeekDates(weekStart: Date): Date[]` - returns array of 7 dates (Mon-Sun)
- `formatWeekRange(weekStart: Date): string` - e.g., "Jan 13 - Jan 19, 2026"
- `formatDayHeader(date: Date): { day: string, date: string }` - e.g., { day: "Mon", date: "13" }
- `getTimeSlots(startHour: number, endHour: number): string[]` - returns array of HH:00 labels
- `addWeeks(date: Date, weeks: number): Date` - utility for week navigation
- `isSameDay(a: Date, b: Date): boolean` - compare dates ignoring time

Use existing types from src/lib/scheduling/types.ts where applicable (SLOT_DURATION_MINUTES, DEFAULT_DAY_START_HOUR, DEFAULT_DAY_END_HOUR).

Export a CalendarViewState type:
```typescript
interface CalendarViewState {
  weekStart: Date
  viewStartHour: number // default 6
  viewEndHour: number   // default 23
}
```
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>Calendar utility functions exported and type-safe</done>
</task>

<task type="auto">
  <name>Task 2: Create TimeColumn component</name>
  <files>src/components/calendar/time-column.tsx</files>
  <action>
Create a TimeColumn component that displays hour labels on the left side of the grid.

Props:
- startHour: number (default 6)
- endHour: number (default 23)

Structure:
- Fixed width column (~60px)
- Display hour labels (6 AM, 7 AM, ... 11 PM) at each hour mark
- Each hour has 4 x 15-minute slots (height matches grid cells)
- Hour labels positioned at the top of each hour block
- Use text-xs text-muted-foreground for labels
- Empty header cell aligned with day headers

Styling:
- Use Tailwind: sticky left-0 z-10 bg-background
- Border-r for separation from day columns
- Each slot height: h-[15px] or similar to match 15-min grid
  </action>
  <verify>npm run build succeeds</verify>
  <done>TimeColumn component renders hour labels correctly</done>
</task>

<task type="auto">
  <name>Task 3: Create WeekGrid component and calendar page</name>
  <files>src/components/calendar/week-grid.tsx, src/app/(protected)/calendar/page.tsx, src/app/(protected)/calendar/layout.tsx</files>
  <action>
Create WeekGrid component:

Props:
- weekStart: Date
- events?: ScheduleEvent[] (from scheduling types, optional for now)
- onEventClick?: (event: ScheduleEvent) => void

Structure:
- Header row: 7 day columns with day name + date (Mon 13, Tue 14, etc.)
- Highlight today's column with subtle bg-accent/10
- Grid area: 7 columns, each with 15-min slot rows
- Use CSS Grid: grid-cols-[60px_repeat(7,1fr)]
- Scrollable container for time slots (overflow-y-auto)
- Current time indicator: red line across the current time slot (only if viewing current week)

Each 15-min slot:
- Hover state for visual feedback (bg-accent/5 on hover)
- Border-b border-border/30 for slot separation
- Thicker border every 4 slots (hour boundary)

Create calendar layout:
- Pass-through layout for calendar-specific styling

Create calendar page:
- Client component with useState for weekStart
- Week navigation: Previous/Next week buttons + "Today" button
- Display week range in header (e.g., "Week of Jan 13 - 19, 2026")
- Limit navigation: current week to +4 weeks forward, no backward past current week
- Fetch schedule on mount and week change (placeholder for now, just log)
- Show loading skeleton while fetching

Use existing Button component from shadcn/ui. Import ChevronLeft, ChevronRight, Calendar from lucide-react.
  </action>
  <verify>
npm run build succeeds
Navigate to http://localhost:3000/calendar shows week grid
  </verify>
  <done>Week grid displays with day headers, time slots, and week navigation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] Calendar page accessible at /calendar
- [ ] Week grid shows 7 columns (Mon-Sun)
- [ ] Time column shows hours 6 AM - 11 PM
- [ ] Week navigation works (next/prev/today)
- [ ] Cannot navigate before current week
- [ ] Can navigate up to 4 weeks ahead
- [ ] Today's column is highlighted
- [ ] Current time indicator appears when viewing current week
</verification>

<success_criteria>

- All tasks completed
- Calendar grid renders correctly with 15-min slots
- Week navigation functional within bounds
- No TypeScript errors
- Ready for event display in Plan 06-02
  </success_criteria>

<output>
After completion, create `.planning/phases/06-calendar-ui/06-01-SUMMARY.md`
</output>
