---
phase: 11-llm-gateway-activation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false
user_setup:
  - service: anthropic
    why: "Primary LLM provider for chat and parsing"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → Settings → API Keys → Create Key"
    account_setup:
      - url: "https://console.anthropic.com"
        skip_if: "Already have Anthropic account with API access"
---

<objective>
Test LLM gateway endpoints with real Anthropic API calls.

Purpose: Validate that the integrated AnthropicProvider works correctly with all LLM endpoints (status, parse-goal, explain, chat).

Output: Confirmed working LLM functionality with real API calls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-llm-gateway-activation/11-01-SUMMARY.md

@src/app/api/llm/status/route.ts
@src/app/api/llm/parse-goal/route.ts
@src/app/api/chat/route.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Configure ANTHROPIC_API_KEY in local environment</action>
  <instructions>
    Plan 11-01 integrated the Anthropic provider into the router.
    Now we need a real API key to test it.

    1. Go to https://console.anthropic.com/settings/keys
    2. Create a new API key (or use existing)
    3. Add to your .env.local file:
       ANTHROPIC_API_KEY=sk-ant-...
  </instructions>
  <verification>The next task will verify the key works</verification>
  <resume-signal>Type "done" when API key is configured in .env.local</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Test LLM status endpoint</name>
  <files></files>
  <action>
Start the dev server and test the LLM status endpoint:

1. Start dev server: npm run dev (in background if not running)
2. Test status endpoint:
   curl http://localhost:3000/api/llm/status

Expected response should show:
- available: true
- provider: "anthropic-claude"
- mode: "anthropic" (or similar)

If it shows offline or a different provider, debug the router logic.
  </action>
  <verify>curl response shows provider="anthropic-claude" and available=true</verify>
  <done>LLM status endpoint confirms Anthropic is active</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>LLM gateway integration with Anthropic Claude</what-built>
  <how-to-verify>
    1. Ensure dev server is running: npm run dev
    2. Open browser to: http://localhost:3000
    3. Log in (or create test account)
    4. Navigate to the calendar page
    5. Look for the chat interface/chatbox
    6. Send a test message like: "What's on my schedule today?"
    7. Verify: Response comes back from Claude (not offline message)
    8. Try: "Move my workout to 5pm on Wednesday"
    9. Verify: Claude understands the request (may fail if no schedule, that's OK)
  </how-to-verify>
  <resume-signal>Type "approved" if chat works with Claude, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ANTHROPIC_API_KEY configured in .env.local
- [ ] /api/llm/status returns anthropic-claude provider
- [ ] Chat interface sends/receives messages via Claude
</verification>

<success_criteria>
- All tasks completed
- User verified chat functionality works
- LLM calls actually hit Anthropic API (not mock/offline)
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-gateway-activation/11-02-SUMMARY.md`
</output>
