---
phase: 11-llm-gateway-activation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: false
user_setup:
  - service: anthropic
    why: "Primary LLM provider for chat and parsing"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → Settings → API Keys → Create Key"
    account_setup:
      - url: "https://console.anthropic.com"
        skip_if: "Already have Anthropic account with API access"
---

<objective>
Configure API key and test LLM endpoints with real Anthropic API calls.

Purpose: Validate that the simplified Anthropic-only system works correctly with all LLM endpoints (status, parse-goal, chat).

Output: Confirmed working LLM functionality with real API calls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-llm-gateway-activation/11-01-SUMMARY.md

@src/app/api/llm/status/route.ts
@src/app/api/llm/parse-goal/route.ts
@src/app/api/chat/route.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Configure ANTHROPIC_API_KEY in local environment</action>
  <instructions>
    Plan 11-01 simplified the LLM system to Anthropic-only.
    Now we need a real API key to test it.

    1. Go to https://console.anthropic.com/settings/keys
    2. Create a new API key (or use existing)
    3. Add to your .env.local file:
       ANTHROPIC_API_KEY=sk-ant-api03-...
    4. Restart dev server if running
  </instructions>
  <verification>The next task will verify the key works</verification>
  <resume-signal>Type "done" when API key is configured in .env.local</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Test LLM status endpoint</name>
  <files></files>
  <action>
Start the dev server and test the LLM status endpoint:

1. Start dev server if not running: npm run dev
2. Test status endpoint:
   curl http://localhost:3000/api/llm/status

Expected response:
```json
{
  "available": true,
  "provider": "anthropic-claude",
  "message": "Using Anthropic Claude API"
}
```

If available is false, check:
- ANTHROPIC_API_KEY is set in .env.local
- Dev server was restarted after adding the key
  </action>
  <verify>curl response shows available=true and provider="anthropic-claude"</verify>
  <done>LLM status endpoint confirms Anthropic is active</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Simplified LLM system with Anthropic Claude</what-built>
  <how-to-verify>
    1. Ensure dev server is running: npm run dev
    2. Open browser to: http://localhost:3000
    3. Log in (or create test account if needed)
    4. Complete onboarding if not done (needed for schedule)
    5. Navigate to the calendar page
    6. Look for the chat interface (chatbox)
    7. Send a test message: "What's on my schedule today?"
    8. Verify: Response comes from Claude (not "offline" or error)
    9. Check browser console for any errors
  </how-to-verify>
  <resume-signal>Type "approved" if chat works, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ANTHROPIC_API_KEY configured in .env.local
- [ ] /api/llm/status returns available=true, provider="anthropic-claude"
- [ ] Chat interface sends/receives messages via Claude
- [ ] No console errors related to LLM
</verification>

<success_criteria>
- All tasks completed
- User verified chat functionality works
- LLM calls hit real Anthropic API
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-gateway-activation/11-02-SUMMARY.md`
</output>
