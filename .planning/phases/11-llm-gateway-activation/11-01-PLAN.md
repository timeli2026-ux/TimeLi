---
phase: 11-llm-gateway-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/llm/router.ts, src/lib/llm/config.ts, .env.example]
autonomous: true
---

<objective>
Wire the Anthropic Claude provider into the LLM router and document configuration.

Purpose: The AnthropicProvider was built in Phase 8 but never integrated into the router - it's exported but unused. This plan connects it as the primary provider when ANTHROPIC_API_KEY is configured.

Output: Working LLM routing that uses Anthropic Claude as primary provider, with proper documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/llm/router.ts
@src/lib/llm/config.ts
@src/lib/llm/providers/anthropic.ts
@src/lib/env.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate AnthropicProvider into router</name>
  <files>src/lib/llm/router.ts</files>
  <action>
Update getLLMProvider() to check for Anthropic as the PRIMARY provider:

1. Import AnthropicProvider from './providers/anthropic'
2. At the TOP of getLLMProvider (before self-hosted check), add:
   - Check if ANTHROPIC_API_KEY is set
   - If set, instantiate and return AnthropicProvider
3. Keep existing fallback chain: self-hosted → OpenAI → offline

The priority order should be:
1. Anthropic (if ANTHROPIC_API_KEY configured) - PRIMARY
2. Self-hosted vLLM (if LLM_BASE_URL configured and in schedule window)
3. OpenAI fallback (if OPENAI_API_KEY configured and usage remaining)
4. Offline (graceful degradation)

Update getLLMStatus() similarly to report 'anthropic' mode.
  </action>
  <verify>TypeScript compiles without errors: npm run build</verify>
  <done>AnthropicProvider is used when ANTHROPIC_API_KEY is present</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example documentation</name>
  <files>.env.example</files>
  <action>
Add ANTHROPIC_API_KEY to the LLM Configuration section:

```
# Anthropic Claude API (primary LLM provider)
# Get API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
```

Place this BEFORE the self-hosted and OpenAI sections to indicate it's the primary/recommended option.

Also add a comment explaining the priority order:
```
# Provider priority: Anthropic > Self-hosted vLLM > OpenAI fallback > Offline
```
  </action>
  <verify>cat .env.example | grep -A2 ANTHROPIC</verify>
  <done>.env.example documents ANTHROPIC_API_KEY as primary LLM option</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] .env.example contains ANTHROPIC_API_KEY documentation
- [ ] Router imports AnthropicProvider
- [ ] getLLMProvider checks ANTHROPIC_API_KEY first
</verification>

<success_criteria>
- All tasks completed
- AnthropicProvider integrated as primary provider
- Documentation updated
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-gateway-activation/11-01-SUMMARY.md`
</output>
