---
phase: 08-llm-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm/providers/anthropic.ts
  - src/lib/llm/cache.ts
  - src/lib/llm/token-budget.ts
  - src/lib/llm/router.ts
  - src/lib/llm/types.ts
  - src/lib/llm/index.ts
  - supabase/migrations/00009_token_usage.sql
autonomous: true
---

<objective>
Enhance LLM infrastructure with Anthropic Claude provider, response caching, and token budget tracking.

Purpose: Add Claude as the primary LLM provider (better for structured outputs) with caching to reduce costs and token tracking for budget enforcement.
Output: Anthropic provider, cache layer, token budget service, and updated router.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/6.5-schedule-chat/6.5-01-SUMMARY.md

# Existing LLM infrastructure
@src/lib/llm/types.ts
@src/lib/llm/router.ts
@src/lib/llm/providers/openai.ts
@src/lib/anthropic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Anthropic Claude provider</name>
  <files>src/lib/llm/providers/anthropic.ts</files>
  <action>
Create AnthropicProvider implementing LLMProvider interface.
- Use existing @anthropic-ai/sdk (already installed)
- Model: claude-3-haiku-20240307 (fast, cost-effective for structured tasks)
- Implement chat() with message conversion (ChatMessage[] -> Anthropic format)
- Include token counting in response (input_tokens + output_tokens)
- Implement isAvailable() checking ANTHROPIC_API_KEY env var
- Match the pattern from OpenAIProvider in providers/openai.ts
- Add proper error handling with sanitized error messages (no API key leaks)
  </action>
  <verify>npm run build passes without TypeScript errors</verify>
  <done>AnthropicProvider exports and implements LLMProvider interface with chat(), isAvailable(), getName(), getStatus()</done>
</task>

<task type="auto">
  <name>Task 2: Create LLM response cache</name>
  <files>src/lib/llm/cache.ts</files>
  <action>
Create in-memory LRU cache for LLM responses with TTL.
- Use Map with max 100 entries (LRU eviction)
- TTL: 5 minutes (per PRD deduplication spec)
- Cache key: SHA-256 hash of (systemPrompt + userMessage)
- Export functions: getCached(key), setCached(key, response), generateCacheKey(messages)
- Include timestamp for TTL checking
- Auto-prune on get/set operations

Note: In-memory is fine for dev. For production at scale, would use Upstash Redis (but that's Phase 10 scope).
  </action>
  <verify>npm run build passes</verify>
  <done>LLM cache module exports getCached, setCached, generateCacheKey functions</done>
</task>

<task type="auto">
  <name>Task 3: Create token budget tracking</name>
  <files>src/lib/llm/token-budget.ts, supabase/migrations/00009_token_usage.sql</files>
  <action>
Create token usage tracking service for budget enforcement.

Migration (00009_token_usage.sql):
- Create token_usage table: id, user_id, date (DATE), input_tokens, output_tokens, provider, created_at
- Index on (user_id, date) for daily queries
- RLS: users can only read their own usage

Service (token-budget.ts):
- recordTokenUsage(supabase, userId, inputTokens, outputTokens, provider)
- getDailyUsage(supabase, userId) -> { inputTokens, outputTokens, total }
- checkBudget(supabase, userId, tier) -> { allowed: boolean, remaining: number }
- Budget limits from PRD: 100k input / 20k output daily, 3k / 500 per session
- Use (supabase as any) pattern for untyped table (consistent with existing code)
  </action>
  <verify>npm run build passes, migration SQL is valid</verify>
  <done>Token budget service and migration created with recordTokenUsage, getDailyUsage, checkBudget functions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] AnthropicProvider implements LLMProvider correctly
- [ ] Cache module has proper LRU eviction and TTL
- [ ] Token budget service tracks usage per user per day
- [ ] Migration SQL is valid and includes RLS
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- LLM infrastructure ready for new endpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/08-llm-gateway/08-01-SUMMARY.md`
</output>
