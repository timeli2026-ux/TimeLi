---
phase: 08-llm-gateway
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/lib/llm/router.ts
  - src/lib/llm/token-budget.ts
  - src/app/api/llm/usage/route.ts
  - src/app/api/llm/parse-goal/route.ts
  - src/app/api/llm/explain/route.ts
  - src/app/api/llm/confirm-preference/route.ts
autonomous: true
---

<objective>
Implement cost control with token budgets, deduplication, and usage tracking API.

Purpose: Enforce token limits per PRD (100k input/20k output daily, 3k/500 per session) and provide usage visibility.
Output: Budget enforcement in all endpoints, deduplication logic, usage API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan contexts
@.planning/phases/08-llm-gateway/08-01-SUMMARY.md
@.planning/phases/08-llm-gateway/08-02-SUMMARY.md
@.planning/phases/08-llm-gateway/08-03-SUMMARY.md

# Token budget service
@src/lib/llm/token-budget.ts
@src/lib/llm/cache.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session budget tracking to token service</name>
  <files>src/lib/llm/token-budget.ts</files>
  <action>
Extend token budget service with session tracking.

Add:
- Session budget type: { maxInputTokens: 3000, maxOutputTokens: 500 }
- Daily budget type: { maxInputTokens: 100000, maxOutputTokens: 20000 }
- getSessionUsage(sessionId): { inputTokens, outputTokens }
- recordSessionUsage(sessionId, inputTokens, outputTokens): void
- checkSessionBudget(sessionId): { allowed: boolean, inputRemaining, outputRemaining }
- BUDGET_EXCEEDED error type for clear error handling

Session storage:
- In-memory Map with 30-min TTL (sessions expire)
- Session ID = hash(userId + Date.now() / (30 * 60 * 1000))
- Auto-cleanup on access

Integrate with daily budget:
- checkBudget should check BOTH session and daily limits
- Return which limit was hit if exceeded
  </action>
  <verify>npm run build passes</verify>
  <done>Session budget tracking added with 3k/500 limits and TTL</done>
</task>

<task type="auto">
  <name>Task 2: Create usage tracking API endpoint</name>
  <files>src/app/api/llm/usage/route.ts</files>
  <action>
Create GET /api/llm/usage endpoint for usage visibility.

Response format:
```json
{
  "daily": {
    "inputTokens": 45000,
    "outputTokens": 8000,
    "inputLimit": 100000,
    "outputLimit": 20000,
    "inputRemaining": 55000,
    "outputRemaining": 12000,
    "percentUsed": 45
  },
  "session": {
    "inputTokens": 1200,
    "outputTokens": 250,
    "inputLimit": 3000,
    "outputLimit": 500,
    "inputRemaining": 1800,
    "outputRemaining": 250,
    "percentUsed": 50
  },
  "cache": {
    "hitRate": 0.35,
    "savedTokens": 15000
  },
  "resetAt": "2024-01-15T00:00:00Z"
}
```

Implementation:
1. Authenticate user
2. Get daily usage from database
3. Get session usage from memory
4. Calculate cache hit rate from recent requests
5. Return combined usage stats
  </action>
  <verify>npm run build passes</verify>
  <done>GET /api/llm/usage endpoint returns comprehensive usage stats</done>
</task>

<task type="auto">
  <name>Task 3: Integrate budget checks into all LLM endpoints</name>
  <files>src/app/api/llm/parse-goal/route.ts, src/app/api/llm/explain/route.ts, src/app/api/llm/confirm-preference/route.ts, src/lib/llm/router.ts</files>
  <action>
Add budget enforcement to all LLM endpoints.

For each endpoint (parse-goal, explain, confirm-preference):
1. Add session ID extraction (from cookie or generate)
2. Check budget BEFORE calling LLM: checkBudget(supabase, userId) + checkSessionBudget(sessionId)
3. If budget exceeded, return 429 with informative error:
   ```json
   {
     "error": "Token budget exceeded",
     "type": "daily_limit" | "session_limit",
     "resetAt": "2024-01-15T00:00:00Z" | "30 minutes",
     "usage": { ... current usage ... }
   }
   ```
4. After LLM call, record usage to BOTH session and daily trackers
5. Include usage stats in successful response headers (X-Token-Usage)

Update router.ts:
- Add estimateTokens(messages: ChatMessage[]): number helper
- Use tiktoken-like estimation: words / 0.75 for rough count
- Pre-flight budget check before provider selection
  </action>
  <verify>npm run build passes</verify>
  <done>All LLM endpoints enforce daily and session token budgets with informative errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Session budget enforces 3k input / 500 output per 30-min window
- [ ] Daily budget enforces 100k input / 20k output
- [ ] Usage endpoint returns comprehensive stats
- [ ] All LLM endpoints return 429 with clear error when budget exceeded
- [ ] Cache hit rate is tracked and reported
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Cost control fully implemented per PRD requirements
  </success_criteria>

<output>
After completion, create `.planning/phases/08-llm-gateway/08-04-SUMMARY.md`
</output>
