---
phase: 6.5-schedule-chat
plan: 02
type: execute
wave: 2
depends_on: ["6.5-01"]
files_modified:
  - src/components/calendar/chatbox.tsx
  - src/app/(protected)/calendar/page.tsx
autonomous: false
---

<objective>
Build the chatbox UI component for schedule conversations.

Purpose: Replace the placeholder chatbox with a functional chat interface that connects to the Claude API.
Output: Working chatbox component with message display, input, loading states, and conversation persistence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context (chat API exists)
@.planning/phases/6.5-schedule-chat/6.5-01-SUMMARY.md

# Calendar page with placeholder chatbox
@src/app/(protected)/calendar/page.tsx

# UI component patterns
@src/components/calendar/completion-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Chatbox component</name>
  <files>src/components/calendar/chatbox.tsx</files>
  <action>
Create src/components/calendar/chatbox.tsx with:

1. Props interface:
```typescript
interface ChatboxProps {
  weekStart: Date
  className?: string
}
```

2. State management:
- messages: Array of { role: 'user' | 'assistant', content: string, timestamp: string }
- inputValue: string
- isLoading: boolean (for typing indicator)
- error: string | null

3. Load existing conversation on mount (GET /api/chat?weekStart=YYYY-MM-DD or create new endpoint)
   Actually - for simplicity, load on first message send. Start with empty state.

4. Message display:
- User messages: right-aligned, primary background
- Assistant messages: left-aligned, muted background
- Auto-scroll to bottom on new messages
- Empty state: "Ask me anything about your schedule..."

5. Input area:
- Textarea (auto-grow, max 3 lines)
- Send button (disabled when empty or loading)
- Submit on Enter (Shift+Enter for newline)
- Clear input on send

6. Loading state:
- Typing indicator (3 dots animation) when waiting for response
- Disable input while loading

7. Error handling:
- Show error message below input if API fails
- Allow retry

Use shadcn Button, existing Textarea from ui/textarea.tsx. Use lucide-react icons (Send, Bot, User).
  </action>
  <verify>npm run build passes, component exports correctly</verify>
  <done>Chatbox component with messages, input, loading, and error states</done>
</task>

<task type="auto">
  <name>Task 2: Integrate chatbox into calendar page</name>
  <files>src/app/(protected)/calendar/page.tsx</files>
  <action>
Replace the placeholder chatbox in calendar/page.tsx:

1. Import the new Chatbox component
2. Replace the placeholder div (lines ~424-453) with:
```tsx
<Chatbox
  weekStart={weekStart}
  className="hidden xl:flex w-72 flex-col border-l bg-muted/30 flex-shrink-0"
/>
```

3. Pass weekStart so chatbox knows which week's schedule to discuss
4. Keep the same responsive behavior (hidden on <xl screens)
5. Remove the old placeholder content (MessageSquare icon, "Coming soon", disabled input)
  </action>
  <verify>npm run dev shows chatbox on xl screens, input is enabled</verify>
  <done>Chatbox integrated into calendar page, replaces placeholder</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Functional chatbox UI connected to Claude API</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/calendar (must have schedule generated)
    3. Resize browser to xl width (1280px+) to see chatbox
    4. Type a message like "What's on my schedule today?"
    5. Press Enter or click Send
    6. Verify: Message appears in chat, typing indicator shows
    7. Verify: Claude response appears after a moment
    8. Verify: Messages persist (refresh page, they're still there - if conversation API working)
    9. Test error: Try with empty message (should not send)
  </how-to-verify>
  <resume-signal>Type "approved" if chat works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Chatbox component renders in calendar
- [ ] Messages can be sent and received
- [ ] Loading state shows while waiting
- [ ] User approved visual verification
</verification>

<success_criteria>

- All tasks completed
- Chatbox functional with Claude responses
- Human approved chat interaction
- Ready for schedule modification features
  </success_criteria>

<output>
After completion, create `.planning/phases/6.5-schedule-chat/6.5-02-SUMMARY.md`
</output>
