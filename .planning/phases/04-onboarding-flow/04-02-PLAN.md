---
phase: 04-onboarding-flow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/onboarding/steps/timezone-step.tsx
  - src/components/onboarding/steps/sleep-step.tsx
  - src/components/onboarding/steps/meals-step.tsx
  - src/components/onboarding/steps/buffer-step.tsx
  - src/components/onboarding/wizard.tsx
  - src/components/ui/select.tsx
  - src/components/ui/label.tsx
  - src/components/ui/slider.tsx
autonomous: true
---

<objective>
Implement onboarding steps 1-4: timezone selection, sleep window, meal times, and buffer preferences.

Purpose: Capture the user's baseline schedule constraints that the scheduling engine will respect as hard constraints.

Output: Four functional wizard steps with form inputs, validation, and data collection into wizard state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Wizard shell and types from Plan 01
@src/components/onboarding/wizard.tsx
@src/components/onboarding/types.ts

# Available UI components
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add required shadcn UI components</name>
  <files>src/components/ui/select.tsx, src/components/ui/label.tsx, src/components/ui/slider.tsx</files>
  <action>
Install shadcn components needed for onboarding forms:

```bash
npx shadcn@latest add select label slider
```

These provide:
- Select: For timezone dropdown
- Label: For form field labels
- Slider: For duration/buffer inputs

Verify components are added to src/components/ui/.
  </action>
  <verify>Files exist in src/components/ui/, npm run build succeeds</verify>
  <done>Select, Label, and Slider components available for use</done>
</task>

<task type="auto">
  <name>Task 2: Create step components for timezone and sleep</name>
  <files>src/components/onboarding/steps/timezone-step.tsx, src/components/onboarding/steps/sleep-step.tsx</files>
  <action>
Create TimezoneStep component (timezone-step.tsx):
- Props: value (current timezone), onChange (callback)
- "use client" directive
- Card with title "What's your timezone?"
- Description: "We'll use this to schedule events at the right times."
- Select dropdown with common timezones:
  - America/New_York (Eastern)
  - America/Chicago (Central)
  - America/Denver (Mountain)
  - America/Los_Angeles (Pacific)
  - America/Anchorage (Alaska)
  - Pacific/Honolulu (Hawaii)
  - UTC
  - Europe/London
  - Europe/Paris
  - Asia/Tokyo
  - Asia/Shanghai
  - Australia/Sydney
- Default to America/New_York
- Call onChange when selection changes

Create SleepStep component (sleep-step.tsx):
- Props: sleepStart, sleepEnd, onSleepStartChange, onSleepEndChange
- "use client" directive
- Card with title "When do you sleep?"
- Description: "We'll never schedule anything during your sleep hours."
- Two time inputs (HTML time input type):
  - "Bedtime" (sleepStart) - default 23:00
  - "Wake up" (sleepEnd) - default 07:00
- Use Input component with type="time"
- Style inputs in a row with labels

Both components:
- Use Card component for consistent styling
- Use Label for form labels
- Centered text with clear hierarchy
  </action>
  <verify>npm run build succeeds, components export correctly</verify>
  <done>TimezoneStep and SleepStep components render with proper form inputs</done>
</task>

<task type="auto">
  <name>Task 3: Create step components for meals and buffer, integrate all into wizard</name>
  <files>src/components/onboarding/steps/meals-step.tsx, src/components/onboarding/steps/buffer-step.tsx, src/components/onboarding/wizard.tsx</files>
  <action>
Create MealsStep component (meals-step.tsx):
- Props: meals object { breakfast: {start, duration}, lunch: {...}, dinner: {...} }, onChange
- "use client" directive
- Card with title "When do you eat?"
- Description: "We'll keep these times clear for meals."
- Three sections (Breakfast, Lunch, Dinner) each with:
  - Time input for start time
  - Slider for duration (15-90 minutes, step 15)
  - Show duration value next to slider
- Default values: Breakfast 08:00/30min, Lunch 12:00/45min, Dinner 18:30/60min
- Use a grid layout for clean arrangement

Create BufferStep component (buffer-step.tsx):
- Props: bufferMinutes, onChange
- "use client" directive
- Card with title "How much buffer time between activities?"
- Description: "This helps you transition between tasks without feeling rushed."
- Slider from 5-30 minutes (step 5)
- Show current value prominently
- Default: 15 minutes
- Add helpful text explaining what buffer means

Update Wizard component (wizard.tsx):
- Import all four step components
- Replace placeholder step content with actual components
- Pass appropriate props from formData state to each step
- Update formData on step changes
- Steps 0-3 now render real components
- Steps 4-6 remain as placeholders (implemented in Plan 03)
  </action>
  <verify>npm run dev, navigate through steps 1-4 in the wizard, verify form inputs work</verify>
  <done>Steps 1-4 fully functional with data binding to wizard state</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] All four step components render correctly
- [ ] Timezone selection updates wizard state
- [ ] Sleep times can be changed and persist in state
- [ ] Meal times and durations work with slider
- [ ] Buffer slider works with visible value
- [ ] Navigation between steps 1-4 preserves data
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Four step components created with proper form inputs
- Wizard integrates all steps with state management
- Data persists when navigating between steps
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-flow/04-02-SUMMARY.md`
</output>
