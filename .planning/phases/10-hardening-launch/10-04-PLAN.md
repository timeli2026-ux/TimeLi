---
phase: 10-hardening-launch
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified: [.planning/SECURITY_AUDIT.md]
autonomous: true
---

<objective>
Final security audit and verification before launch.

Purpose: Verify all security requirements are met and document the security posture.
Output: SECURITY_AUDIT.md with verification results and any remediation notes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@next.config.ts
@src/middleware.ts
@src/lib/rate-limit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit security implementations</name>
  <files>.planning/SECURITY_AUDIT.md</files>
  <action>
    Create SECURITY_AUDIT.md documenting verification of each security requirement:

    **SEC-01: Environment variable management**
    - Verify: grep for hardcoded API keys in src/
    - Check: All secrets in .env.local, not in code
    - Status: ✅/❌

    **SEC-02: Secrets scanning**
    - Verify: .husky/pre-commit exists with gitleaks
    - Check: .gitleaks.toml has appropriate rules
    - Status: ✅/❌

    **SEC-03: Environment variable validation**
    - Verify: src/lib/env.ts exists with Zod schema
    - Check: App fails fast on missing required vars
    - Status: ✅/❌

    **SEC-04: API key exposure audit**
    - Verify: grep for 'sk_', 'pk_', 'supabase', 'anthropic' in .next/static/
    - Check: No API keys in client bundle
    - Run: npm run build && grep -r "sk_\|SUPABASE_SERVICE\|ANTHROPIC" .next/static/ (should find nothing)
    - Status: ✅/❌

    **SEC-05: Supabase RLS policies**
    - Verify: All tables have RLS enabled
    - Check: Policies exist for profiles, goals, schedules, completions, etc.
    - Status: ✅/❌

    **SEC-06: Input sanitization**
    - Verify: src/lib/sanitize.ts exists
    - Check: All user inputs validated with Zod
    - Status: ✅/❌

    **SEC-07: Security headers**
    - Verify: next.config.ts has headers() function
    - Check: CSP, X-Frame-Options, X-Content-Type-Options present
    - Status: ✅/❌

    **SEC-08: CSRF protection**
    - Verify: All state-changing operations require authentication
    - Check: Supabase auth tokens validate origin
    - Status: ✅/❌

    **SEC-09: Dependency scanning**
    - Verify: .github/dependabot.yml exists
    - Check: npm audit shows no critical vulnerabilities
    - Run: npm audit
    - Status: ✅/❌

    **SEC-10: Rate limiting**
    - Verify: src/lib/rate-limit.ts exists
    - Check: Auth endpoints protected (middleware.ts)
    - Status: ✅/❌

    **SEC-11: Authentication security**
    - Verify: Session tokens expire appropriately
    - Check: Refresh token rotation configured in Supabase
    - Status: ✅/❌

    **SEC-12: Stripe webhook verification**
    - Verify: src/app/api/webhooks/stripe/route.ts uses constructEvent
    - Check: Signature verification before processing
    - Status: ✅/❌

    **SEC-13: Data encryption at rest**
    - Note: Supabase handles this automatically
    - Verify: Supabase project settings show encryption enabled
    - Status: ✅ (managed service)

    **SEC-14: HTTPS enforcement**
    - Note: Vercel handles this automatically
    - Verify: HTTP requests redirect to HTTPS
    - Status: ✅ (managed service)

    **SEC-15: Error handling**
    - Verify: API routes return generic errors to client
    - Check: Stack traces not exposed in production
    - Status: ✅/❌

    **SEC-16: Secure logging**
    - Verify: src/lib/logging/ excludes sensitive data
    - Check: No API keys or passwords logged
    - Status: ✅/❌

    **Summary:**
    - Total requirements: 16
    - Passed: X
    - Failed: Y
    - Notes/Remediation: [any issues found]
  </action>
  <verify>SECURITY_AUDIT.md created with verification results for all 16 requirements</verify>
  <done>Security audit document complete with pass/fail for each requirement</done>
</task>

<task type="auto">
  <name>Task 2: Run automated security checks</name>
  <files>none (verification only)</files>
  <action>
    Run automated security checks and document results:

    1. **npm audit**
       ```bash
       npm audit
       ```
       Document any vulnerabilities found

    2. **Build output audit**
       ```bash
       npm run build
       grep -r "sk_\|service_role\|ANTHROPIC_API_KEY" .next/static/ 2>/dev/null || echo "No secrets in client bundle"
       ```

    3. **Secrets in code audit**
       ```bash
       grep -r "sk_live\|sk_test\|supabase.*service\|anthropic.*sk-" src/ --include="*.ts" --include="*.tsx" 2>/dev/null || echo "No hardcoded secrets"
       ```

    4. **Rate limit verification**
       Test auth endpoint rate limiting (if running locally):
       ```bash
       for i in {1..10}; do curl -s -o /dev/null -w "%{http_code}\n" -X POST localhost:3000/api/auth/login -d '{}'; done
       ```
       Should see 429 after 5 requests

    5. **Header verification**
       ```bash
       curl -I localhost:3000 | grep -E "Content-Security-Policy|X-Frame-Options|X-Content-Type-Options"
       ```

    Update SECURITY_AUDIT.md with results from automated checks.
  </action>
  <verify>All automated checks pass, results documented in SECURITY_AUDIT.md</verify>
  <done>Automated security checks complete with documented results</done>
</task>

<task type="auto">
  <name>Task 3: Update requirements tracking</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update REQUIREMENTS.md to mark completed security requirements:
    - Check off SEC-01 through SEC-16 based on audit results
    - Check off LAND-01, REV-01, REV-02, REV-03
    - Update the progress tracking table
    - Calculate final coverage percentage
  </action>
  <verify>REQUIREMENTS.md reflects final status of Phase 10 requirements</verify>
  <done>Requirements tracking updated with Phase 10 completion status</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] SECURITY_AUDIT.md exists with all 16 requirements verified
- [ ] npm audit shows no critical vulnerabilities
- [ ] No secrets found in client bundle
- [ ] REQUIREMENTS.md updated with Phase 10 status
- [ ] All security headers present and working
</verification>

<success_criteria>

- All tasks completed
- Security audit documented
- No critical security issues found
- Requirements tracking updated
- Project ready for launch
  </success_criteria>

<output>
After completion, create `.planning/phases/10-hardening-launch/10-04-SUMMARY.md`
</output>
