---
phase: 10-hardening-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/app/(protected)/review/page.tsx, src/app/api/review/route.ts, src/components/review/completion-summary.tsx, src/components/review/preference-suggestions.tsx, src/app/(protected)/layout.tsx]
autonomous: true
---

<objective>
Build weekly review feature showing completion summary, preference suggestions, and notes.

Purpose: Help users reflect on their week, see patterns, and improve future schedules.
Output: /review page with completion stats, pattern-based suggestions, and notes section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/schedule/complete/route.ts
@src/components/calendar/goal-progress.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create review API endpoint</name>
  <files>src/app/api/review/route.ts, src/app/api/review/notes/route.ts</files>
  <action>
    1. **GET /api/review** - Fetch weekly review data:
       - Query schedule_completions for the specified week (default: last completed week)
       - Calculate stats:
         - Total scheduled events
         - Completed count, skipped count, partial count
         - Completion rate percentage
         - Most productive time slots (group completions by hour, find top 3)
         - Completions by realm (join with goals/realms for breakdown)
       - Return: { stats, completionsByRealm, productiveTimes, weekStart }

    2. **POST /api/review/notes** - Save weekly notes:
       - Accept { weekStart, notes } body
       - Upsert into generated_schedules table (add notes column if not exists)
       - Return 200 on success

    3. **Add notes column migration** if needed:
       - ALTER TABLE generated_schedules ADD COLUMN IF NOT EXISTS review_notes text

    Use (supabase as any) pattern for untyped queries.
  </action>
  <verify>curl localhost:3000/api/review returns JSON with stats object</verify>
  <done>Review API returns completion stats, productive times, and realm breakdown</done>
</task>

<task type="auto">
  <name>Task 2: Build weekly review page</name>
  <files>src/app/(protected)/review/page.tsx, src/components/review/completion-summary.tsx, src/components/review/preference-suggestions.tsx</files>
  <action>
    1. **Create /review page**:
       - Week selector (previous weeks only - can't review incomplete weeks)
       - Three sections: Summary, Suggestions, Notes
       - Loading skeleton while fetching data

    2. **CompletionSummary component**:
       - Large completion rate percentage (circular progress or big number)
       - Breakdown: X completed, Y skipped, Z partial
       - Completions by realm (small horizontal bars with realm colors)
       - Most productive times (list top 3 time slots)

    3. **PreferenceSuggestions component**:
       - Analyze completion data for patterns:
         - If completions high in morning → "You seem most productive in the morning"
         - If specific realm has low completion → "Consider reducing [realm] goals"
         - If pattern of skipped events at certain times → "Events at [time] are often skipped"
       - Present as actionable suggestions with confirm/dismiss buttons
       - Confirming saves as preference (future enhancement - for now just dismiss)

    4. **Notes section**:
       - Textarea for weekly reflection
       - Auto-save on blur (debounced)
       - Persist to generated_schedules.review_notes

    5. **Add "Review" to protected layout navigation** (sidebar/nav)
  </action>
  <verify>npm run build succeeds, /review page loads with completion data</verify>
  <done>Weekly review page shows completion stats, suggestions, and editable notes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] /review page accessible from protected layout
- [ ] Completion summary shows accurate percentages
- [ ] Productive times displayed
- [ ] Notes save and persist on refresh
- [ ] Works with no completion data (graceful empty state)
</verification>

<success_criteria>

- All tasks completed
- REV-01: Completion summary and productive times displayed
- REV-02: Preference suggestions from patterns presented
- REV-03: Notes section available
- Review accessible from main navigation
  </success_criteria>

<output>
After completion, create `.planning/phases/10-hardening-launch/10-02-SUMMARY.md`
</output>
