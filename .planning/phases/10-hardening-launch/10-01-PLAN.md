---
phase: 10-hardening-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/page.tsx, src/app/api/waitlist/route.ts]
autonomous: true
---

<objective>
Create a polished landing page with Linear/Cal.com aesthetic and waitlist signup.

Purpose: First impression for visitors - clean, professional, and conversion-focused.
Output: Landing page at / with waitlist form that captures emails to Supabase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create waitlist API endpoint</name>
  <files>src/app/api/waitlist/route.ts, supabase/migrations/00012_waitlist.sql</files>
  <action>
    1. Create migration for waitlist table:
       - id (uuid, primary key)
       - email (text, unique, not null)
       - created_at (timestamptz, default now())
       - Enable RLS with INSERT policy for anon (anyone can join waitlist)

    2. Create POST /api/waitlist endpoint:
       - Accept { email } in body
       - Validate email format with Zod
       - Insert into waitlist table
       - Return 201 on success, 409 if email exists, 400 on validation error
       - Use anon key (no auth required for waitlist)
  </action>
  <verify>curl -X POST localhost:3000/api/waitlist -d '{"email":"test@test.com"}' returns 201 or 409</verify>
  <done>Waitlist endpoint saves emails to database with validation</done>
</task>

<task type="auto">
  <name>Task 2: Build landing page with Linear/Cal.com aesthetic</name>
  <files>src/app/page.tsx</files>
  <action>
    Create landing page with these sections:

    1. **Hero Section** (above the fold):
       - Headline: "Your week, optimized." or similar concise value prop
       - Subheadline: Brief explanation of AI-powered weekly scheduling
       - Waitlist form: email input + "Join waitlist" button
       - Simple, centered layout with generous whitespace

    2. **Features Section** (3 key features):
       - "Understands your constraints" - locked events, sleep, meals
       - "Explains its reasoning" - rationale for every placement
       - "Learns your patterns" - preferences confirmed over time
       - Use Lucide icons for visual interest

    3. **How It Works** (3 steps):
       - "Tell us about your life" (onboarding)
       - "We build your schedule" (algorithm)
       - "Refine with AI chat" (chat feature)

    4. **Footer**: Simple footer with copyright

    **Design Guidelines:**
    - Use existing Geist font from layout.tsx
    - Dark/light mode support via Tailwind dark: classes
    - Zinc color palette (matching existing app)
    - Minimal animations (subtle hover states only)
    - Mobile-responsive (stack on small screens)
    - No external images - use Lucide icons and CSS gradients only

    **Form State:**
    - useState for email input and submission state
    - Loading state during API call
    - Success message: "You're on the list!"
    - Error handling for duplicate/invalid emails
  </action>
  <verify>npm run build succeeds, page renders at localhost:3000 with all sections visible</verify>
  <done>Landing page displays hero, features, how it works, and working waitlist form</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] Landing page renders at localhost:3000/
- [ ] Waitlist form submits successfully (check Supabase for entry)
- [ ] Page looks good on mobile (375px) and desktop (1440px)
- [ ] Dark mode works correctly
</verification>

<success_criteria>

- All tasks completed
- Landing page matches Linear/Cal.com aesthetic (clean, minimal, professional)
- Waitlist captures emails to database
- No errors or warnings in console
  </success_criteria>

<output>
After completion, create `.planning/phases/10-hardening-launch/10-01-SUMMARY.md`
</output>
