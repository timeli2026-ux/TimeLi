---
phase: 11-database-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00015_assignments_table.sql
  - src/app/api/assignments/route.ts
  - src/app/api/assignments/[id]/route.ts
  - src/lib/validations/assignments.ts
autonomous: true
---

<objective>
Create assignments table and CRUD API for student assignment tracking.

Purpose: Enable students to track assignments with due dates and estimated hours for semester-wide scheduling.
Output: assignments table with RLS, validation schema, and full CRUD API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing patterns to follow:
@supabase/migrations/00004_life_realms.sql
@src/app/api/goals/route.ts
@src/lib/validations/goals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assignments table migration</name>
  <files>supabase/migrations/00015_assignments_table.sql</files>
  <action>
Create migration file with:

**Table: assignments**
- id: uuid primary key (gen_random_uuid())
- user_id: uuid FK to auth.users (cascade delete), not null
- course_id: uuid FK to courses (cascade delete), nullable (allows standalone assignments)
- title: text not null (e.g., "Problem Set 3", "Final Exam")
- type: text not null (enum-like: 'homework', 'exam', 'project', 'reading', 'quiz', 'paper', 'other')
- due_date: timestamptz not null
- estimated_hours: numeric(4,1) not null check (>= 0.5 and <= 100)
- priority: text default 'medium' check in ('high', 'medium', 'low')
- notes: text (optional)
- status: text default 'pending' check in ('pending', 'in_progress', 'completed')
- completed_at: timestamptz (set when status changes to completed)
- created_at, updated_at: timestamptz

**Why course_id nullable:** Users may have assignments not tied to a specific course (personal deadlines, external exams).

**RLS policies** (user-owns-row pattern):
- SELECT: auth.uid() = user_id
- INSERT: auth.uid() = user_id
- UPDATE: auth.uid() = user_id (using + with check)
- DELETE: auth.uid() = user_id

**Indexes:**
- assignments_user_id_idx on (user_id)
- assignments_course_id_idx on (course_id) where course_id is not null
- assignments_due_date_idx on (user_id, due_date)
- assignments_status_idx on (user_id, status)

**Trigger:** updated_at using existing update_updated_at() function
  </action>
  <verify>Run `npx supabase db push --dry-run` or check SQL syntax is valid</verify>
  <done>Migration file exists with table, RLS policies, indexes, and trigger</done>
</task>

<task type="auto">
  <name>Task 2: Create assignment validation schema</name>
  <files>src/lib/validations/assignments.ts</files>
  <action>
Create Zod validation schemas following goals.ts pattern:

**assignmentTypeEnum:** z.enum(['homework', 'exam', 'project', 'reading', 'quiz', 'paper', 'other'])

**assignmentPriorityEnum:** z.enum(['high', 'medium', 'low'])

**assignmentStatusEnum:** z.enum(['pending', 'in_progress', 'completed'])

**assignmentFormSchema:**
- title: string, min 1, max 200
- courseId: string uuid optional nullable (use .nullable() for optional FK)
- type: assignmentTypeEnum
- dueDate: string datetime (ISO format)
- estimatedHours: number, min 0.5, max 100
- priority: assignmentPriorityEnum optional, default 'medium'
- notes: string optional, max 1000
- status: assignmentStatusEnum optional, default 'pending'

**assignmentUpdateSchema:** partial of assignmentFormSchema

**assignmentResponseSchema:** assignmentFormSchema extended with:
- id, userId, createdAt, updatedAt, completedAt (nullable)
- courseName (optional - for joined queries)

Export TypeScript types: AssignmentFormData, AssignmentUpdateData, AssignmentResponse, AssignmentType, AssignmentPriority, AssignmentStatus
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/lib/validations/assignments.ts`</verify>
  <done>Validation schema exports types and schemas matching DB structure</done>
</task>

<task type="auto">
  <name>Task 3: Create assignments API routes</name>
  <files>src/app/api/assignments/route.ts, src/app/api/assignments/[id]/route.ts</files>
  <action>
**GET/POST /api/assignments (route.ts):**
Follow goals/route.ts pattern:
- GET: Authenticate, query assignments with optional course join for courseName
  - Accept query params: ?courseId=uuid (filter by course), ?status=pending (filter by status)
  - Order by due_date ascending (upcoming first)
  - Return { assignments: [...] }
- POST: Authenticate, validate body with assignmentFormSchema, sanitize title/notes, insert, return { assignment: {...} }

**GET/PUT/DELETE /api/assignments/[id] ([id]/route.ts):**
- GET: Authenticate, fetch by id with course join for courseName, return { assignment }
- PUT: Authenticate, validate body with assignmentUpdateSchema
  - If status changing to 'completed', set completed_at = now()
  - If status changing from 'completed', set completed_at = null
  - Return { assignment }
- DELETE: Authenticate, delete by id, return { success: true }

Use (supabase as any) cast for queries.
Transform snake_case to camelCase in responses.
Sanitize title and notes fields with sanitizeUserInput.
  </action>
  <verify>Build succeeds: `npm run build`</verify>
  <done>Full CRUD API for assignments at /api/assignments and /api/assignments/[id]</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration file is valid SQL (no syntax errors)
- [ ] `npm run build` succeeds without TypeScript errors
- [ ] Validation schema exports all required types
- [ ] API routes follow established patterns
- [ ] Query filtering works (courseId, status params)
</verification>

<success_criteria>

- All tasks completed
- assignments table ready for migration
- CRUD API endpoints functional with filtering
- Status transitions properly update completed_at
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/11-database-models/11-02-SUMMARY.md`
</output>
