---
phase: 01-foundation-security-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, components.json, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/lib/utils.ts]
autonomous: true
---

<objective>
Create Next.js 15 project with TypeScript, Tailwind CSS, and shadcn/ui base components.

Purpose: Establish the foundational project structure that all subsequent phases build upon.
Output: Working Next.js 15 app with TypeScript, Tailwind styling, and shadcn/ui components ready for use.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, .gitignore</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` to create Next.js 15 project in current directory.

    After creation:
    - Verify App Router structure exists in src/app/
    - Verify TypeScript is configured (tsconfig.json)
    - Verify Tailwind is configured (tailwind.config.ts)
    - Clean up default page.tsx to minimal "TimeLi" placeholder

    Do NOT add any custom code beyond minimal placeholder. This is scaffolding only.
  </action>
  <verify>npm run build succeeds without errors</verify>
  <done>Next.js 15 project runs, TypeScript compiles, Tailwind processes styles</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure shadcn/ui</name>
  <files>components.json, src/lib/utils.ts, src/app/globals.css</files>
  <action>
    Run `npx shadcn@latest init` with these choices:
    - Style: Default
    - Base color: Neutral (clean aesthetic per PRD)
    - CSS variables: Yes
    - tailwind.config location: tailwind.config.ts
    - components alias: @/components
    - utils alias: @/lib/utils

    This creates components.json config and src/lib/utils.ts with cn() helper.

    Update globals.css if needed to ensure CSS variables are properly set up for light/dark mode support.
  </action>
  <verify>components.json exists, src/lib/utils.ts exports cn function</verify>
  <done>shadcn/ui initialized with proper configuration</done>
</task>

<task type="auto">
  <name>Task 3: Add base shadcn/ui components</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx</files>
  <action>
    Install essential base components that will be used across the app:

    ```bash
    npx shadcn@latest add button card input
    ```

    These provide:
    - Button: Primary interaction component
    - Card: Container component for content sections
    - Input: Form input component

    Do NOT install all components - add others as needed in later phases.
  </action>
  <verify>npm run build succeeds, component files exist in src/components/ui/</verify>
  <done>Button, Card, Input components available for use</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts development server
- [ ] Browser shows TimeLi placeholder at localhost:3000
- [ ] TypeScript compilation passes (`npx tsc --noEmit`)
- [ ] shadcn/ui components import correctly
</verification>

<success_criteria>

- All tasks completed
- Next.js 15 project builds and runs
- TypeScript configured and compiling
- Tailwind CSS processing styles
- shadcn/ui initialized with base components
- Clean project structure in src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-base/01-01-SUMMARY.md`
</output>
