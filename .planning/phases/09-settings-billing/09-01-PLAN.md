---
phase: 09-settings-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00010_billing_tables.sql
  - src/lib/env.ts
  - src/app/(protected)/settings/page.tsx
  - src/app/(protected)/settings/account/page.tsx
  - src/components/settings/account-section.tsx
autonomous: true
user_setup:
  - service: stripe
    why: "Payment processing for subscriptions"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys → Secret key"
      - name: STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard → Developers → API keys → Publishable key"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard → Developers → Webhooks → Signing secret (after creating endpoint)"
      - name: STRIPE_PRICE_ID
        source: "Stripe Dashboard → Products → Create product → Copy price ID"
    account_setup:
      - url: "https://dashboard.stripe.com/register"
        skip_if: "Already have Stripe account"
    dashboard_config:
      - task: "Create monthly subscription product"
        location: "Stripe Dashboard → Products → Add product"
        details: "Name: TimeLi Monthly, Price: $15/month recurring"
      - task: "Create webhook endpoint (after deploy)"
        location: "Stripe Dashboard → Developers → Webhooks → Add endpoint"
        details: "URL: https://[your-domain]/api/webhooks/stripe, Events: checkout.session.completed, customer.subscription.created, customer.subscription.updated, customer.subscription.deleted, invoice.paid, invoice.payment_failed"
    local_dev:
      - "stripe listen --forward-to localhost:3000/api/webhooks/stripe"
---

<objective>
Create billing database tables, settings page foundation, and account management section.

Purpose: Establish the database schema for subscriptions and usage tracking, and create the account settings UI.
Output: Database migrations, settings layout with tabs, and account management component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-billing/DISCOVERY.md

@src/app/(protected)/settings/preferences/page.tsx
@src/lib/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create billing database tables</name>
  <files>supabase/migrations/00010_billing_tables.sql</files>
  <action>
Create migration with:
1. `subscriptions` table:
   - id (uuid primary key)
   - user_id (uuid references auth.users, unique, cascade delete)
   - stripe_customer_id (text, unique, nullable)
   - stripe_subscription_id (text, unique, nullable)
   - status (text: 'inactive', 'trialing', 'active', 'canceled', 'past_due')
   - plan (text default 'monthly')
   - price_cents (integer default 1500)
   - trial_start, trial_end (timestamptz nullable)
   - current_period_start, current_period_end (timestamptz nullable)
   - cancel_at_period_end (boolean default false)
   - created_at, updated_at timestamps

2. `usage_tracking` table:
   - id (uuid primary key)
   - user_id (uuid references auth.users, cascade delete)
   - period_start, period_end (date)
   - schedule_generations (integer default 0)
   - recalibrations (integer default 0)
   - llm_requests (integer default 0)
   - unique constraint on (user_id, period_start)

3. Enable RLS with user-owns-row pattern for both tables
4. Add updated_at triggers
  </action>
  <verify>Migration file exists with valid SQL syntax</verify>
  <done>Both tables defined with RLS policies and triggers</done>
</task>

<task type="auto">
  <name>Task 2: Add Stripe env vars to validation</name>
  <files>src/lib/env.ts, .env.example</files>
  <action>
Add to env schema (all optional for development):
- STRIPE_SECRET_KEY (string, optional)
- STRIPE_PUBLISHABLE_KEY (string, optional)
- STRIPE_WEBHOOK_SECRET (string, optional)
- STRIPE_PRICE_ID (string, optional)
- USAGE_LIMIT_GENERATIONS (number, default 200)
- USAGE_LIMIT_RECALIBRATIONS (number, default 500)

Update .env.example with Stripe section and usage limits.
  </action>
  <verify>npm run build succeeds with new env vars (optional means app still runs without them)</verify>
  <done>Env schema includes Stripe vars, .env.example updated</done>
</task>

<task type="auto">
  <name>Task 3: Create settings page layout and account section</name>
  <files>src/app/(protected)/settings/page.tsx, src/app/(protected)/settings/account/page.tsx, src/components/settings/account-section.tsx</files>
  <action>
1. Create `/settings/page.tsx`:
   - Redirect to /settings/preferences (default tab)

2. Create `/settings/account/page.tsx`:
   - Same header structure as preferences page
   - Tab navigation: Preferences | Goals | Account | Subscription
   - Account tab active
   - Contains AccountSection component

3. Create `AccountSection` component:
   - Display user email (from session)
   - Display timezone (from profile)
   - "Change Password" button (links to /forgot-password for now - full implementation later)
   - "Delete Account" section with warning and confirmation dialog
   - Delete calls DELETE /api/account (implementation in next task)

Use existing patterns from preferences page for consistency. Use shadcn Dialog for delete confirmation.
  </action>
  <verify>Pages render at /settings, /settings/account without errors</verify>
  <done>Settings layout complete, account section shows user info and delete option</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration file creates both tables with RLS
- [ ] npm run build succeeds
- [ ] /settings redirects to /settings/preferences
- [ ] /settings/account renders with user email and timezone
- [ ] Delete account dialog appears with confirmation
</verification>

<success_criteria>

- Database schema ready for billing
- Env validation includes Stripe variables
- Settings page has proper tab navigation
- Account section displays user info
- Delete account UI ready (API in next plan)
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-billing/09-01-SUMMARY.md`
</output>
