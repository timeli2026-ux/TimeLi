---
phase: 07-goals-preferences
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/api/preferences/route.ts
  - src/app/(protected)/settings/preferences/page.tsx
  - src/components/preferences/preferences-panel.tsx
  - src/components/preferences/schedule-preferences-form.tsx
autonomous: true
---

<objective>
Create preferences panel for editing global scheduling preferences.

Purpose: Allow users to modify their scheduling preferences (sleep, meals, commute, chronotype) after onboarding.
Output: Working preferences page accessible from settings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Database schema for preferences
@supabase/migrations/cleanup_and_recreate.sql
@src/types/database.ts

# Existing patterns
@src/components/onboarding/steps/sleep-step.tsx
@src/components/onboarding/steps/meals-step.tsx
@src/app/api/onboarding/complete/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create preferences API route</name>
  <files>src/app/api/preferences/route.ts</files>
  <action>
Create /api/preferences endpoint:

**GET**: Fetch current user preferences
- Query user_preferences for authenticated user
- Return all preference fields (sleep, meals, commute, chronotype, buffer)
- 404 if no preferences found

**PUT**: Update user preferences
- Validate input with Zod schema (partial update allowed)
- Fields to support:
  - timezone: string
  - sleepStart, sleepEnd: time strings (HH:mm)
  - weekendSleepStart, weekendSleepEnd: optional time strings
  - chronotype: 'early_bird' | 'night_owl' | 'intermediate'
  - bufferMinutes: number 5-30
  - mealBreakfastStart, mealBreakfastDuration: optional
  - mealLunchStart, mealLunchDuration: optional
  - mealDinnerStart, mealDinnerDuration: optional
  - commuteMorningStart, commuteMorningDuration: optional
  - commuteEveningStart, commuteEveningDuration: optional
- Update user_preferences for authenticated user
- Return updated preferences

Use createClient from @/lib/supabase/server.
Use (supabase as any) for tables not in generated types.
  </action>
  <verify>TypeScript compiles: npx tsc --noEmit</verify>
  <done>GET and PUT /api/preferences work with proper auth and validation</done>
</task>

<task type="auto">
  <name>Task 2: Create schedule preferences form component</name>
  <files>src/components/preferences/schedule-preferences-form.tsx</files>
  <action>
Create comprehensive preferences form with sections:

**1. Sleep Schedule Section**
- Weekday: bedtime and wake time (time inputs)
- Weekend (optional): separate bedtime and wake time
- Toggle: "Same as weekday" for weekend

**2. Energy Profile Section**
- Chronotype selector: Early Bird / Night Owl / Intermediate
- Description text explaining each type
- Visual timeline showing peak/trough hours

**3. Meals Section**
- Breakfast: toggle enabled + time + duration slider
- Lunch: toggle enabled + time + duration slider
- Dinner: toggle enabled + time + duration slider
- Note: "Meals are blocked as unavailable time in your schedule"

**4. Commute Section**
- Morning commute: toggle enabled + time + duration slider
- Evening commute: toggle enabled + time + duration slider

**5. Buffer Time Section**
- Slider: 5-30 minutes
- Explanation: "Time between activities for transitions"

Form behavior:
- Props: initialData, onSubmit, isSubmitting
- Use react-hook-form with Zod resolver (or controlled inputs)
- Submit button: "Save Preferences"
- Reset button: "Discard Changes"
- Show unsaved changes indicator

Reuse components from onboarding steps where possible (time inputs, sliders).
  </action>
  <verify>Component compiles: npx tsc --noEmit</verify>
  <done>SchedulePreferencesForm renders all preference sections with proper inputs</done>
</task>

<task type="auto">
  <name>Task 3: Create preferences page</name>
  <files>src/app/(protected)/settings/preferences/page.tsx, src/components/preferences/preferences-panel.tsx</files>
  <action>
**src/components/preferences/preferences-panel.tsx**:
Create preferences panel wrapper:
- Fetches preferences on mount
- Loading state: skeleton form
- Error state: retry button
- Success: renders SchedulePreferencesForm
- Handles submit: calls PUT /api/preferences
- Shows toast on success/error
- Updates local state on successful save

**src/app/(protected)/settings/preferences/page.tsx**:
Create preferences page at /settings/preferences:

1. Page header:
   - Title: "Schedule Preferences"
   - Subtitle: "Customize how your week is scheduled"
   - Back link to /settings (or /calendar)

2. Info banner:
   - "Changes to preferences will affect future schedule generations"
   - Option to regenerate current week after saving

3. Main content:
   - PreferencesPanel component
   - Centered layout with max-width

4. Navigation:
   - Tabs or links: Preferences | Goals | Account (for future settings pages)

Responsive:
- Full width on mobile
- Max-w-2xl centered on desktop
  </action>
  <verify>
Page renders correctly:
- `npm run dev` and visit /settings/preferences
- Page loads user's current preferences
- Form submits and shows success toast
  </verify>
  <done>Preferences page allows editing all scheduling preferences</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] GET /api/preferences returns user's preferences
- [ ] PUT /api/preferences updates and returns preferences
- [ ] /settings/preferences page renders form with current values
- [ ] Saving preferences shows success feedback
</verification>

<success_criteria>
- All tasks completed
- Preferences API functional
- Form displays all preference categories
- Changes persist correctly
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-goals-preferences/07-03-SUMMARY.md`
</output>
