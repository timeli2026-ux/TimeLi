---
phase: 07-goals-preferences
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(protected)/goals/page.tsx
  - src/components/goals/goal-card.tsx
  - src/components/goals/goal-form-dialog.tsx
  - src/components/goals/goals-list.tsx
  - src/lib/hooks/use-goals.ts
autonomous: true
---

<objective>
Create goals management UI with list view, card components, and add/edit dialog.

Purpose: Allow users to view, add, edit, and delete their goals from a dedicated goals page.
Output: Working /goals page with full CRUD functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# API from Plan 01
@src/app/api/goals/route.ts
@src/lib/validations/goals.ts

# UI patterns
@src/app/(protected)/calendar/page.tsx
@src/components/calendar/dashboard-sidebar.tsx
@src/components/onboarding/steps/actions-step.tsx

# shadcn components available
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useGoals hook and goals list component</name>
  <files>src/lib/hooks/use-goals.ts, src/components/goals/goals-list.tsx</files>
  <action>
**src/lib/hooks/use-goals.ts**:
Create React Query-style custom hook (using useState/useEffect, no external deps):
- `useGoals()` returns { goals, isLoading, error, refetch, createGoal, updateGoal, deleteGoal }
- Fetches from /api/goals on mount
- Provides mutation functions that call API and refetch
- Handle migration check: if goals empty and not loading, call /api/goals/migrate once

**src/components/goals/goals-list.tsx**:
Create goals list container component:
- Groups goals by realm
- Shows realm header with icon and name
- Shows count of goals per realm and total hours
- Empty state: "No goals yet. Add your first goal to start scheduling."
- Loading state: skeleton cards
- Maps goals to GoalCard components (created in Task 2)

Layout:
- Full width container
- Realm sections with dividers
- Responsive grid: 1 col mobile, 2 cols tablet, 3 cols desktop
  </action>
  <verify>Components compile: npx tsc --noEmit</verify>
  <done>useGoals hook fetches and mutates goals, GoalsList renders grouped goals</done>
</task>

<task type="auto">
  <name>Task 2: Create goal card and form dialog components</name>
  <files>src/components/goals/goal-card.tsx, src/components/goals/goal-form-dialog.tsx</files>
  <action>
**src/components/goals/goal-card.tsx**:
Create card component displaying single goal:
- Title prominently displayed
- Stats row: hours/week, frequency badge, intensity dots
- Realm badge with color (use REALM_COLORS from calendar)
- Cognitive load indicator (brain icon with high/med/low)
- Deadline badge if set (calendar icon, "Due: date")
- Action buttons: Edit (pencil), Archive (archive icon), Delete (trash)
- Use shadcn Card, Button, Badge components
- Muted styling for archived goals (is_active: false)

**src/components/goals/goal-form-dialog.tsx**:
Create dialog for add/edit goal:
- Props: open, onOpenChange, goal (optional for edit mode), realms, onSubmit
- Form fields matching goalFormSchema:
  - Title (Input)
  - Realm (Select dropdown)
  - Hours per week (Slider or Input)
  - Cognitive load (Select: High/Medium/Low)
  - Requires deep work (Checkbox)
  - Deadline (optional date picker or Input type="date")
  - Deadline type (Select: Hard/Soft/None)
  - Preferred time (Select: Morning/Afternoon/Evening/Any)
  - Excluded days (multi-select checkboxes for Mon-Sun)
  - Session duration (Slider: min/preferred)
  - Intensity (Slider 1-5)
- Use shadcn Dialog, Form components
- Submit button: "Add Goal" or "Save Changes"
- Cancel button closes dialog
- Validate with goalFormSchema before submit
  </action>
  <verify>Components compile: npx tsc --noEmit</verify>
  <done>GoalCard displays goal info with actions, GoalFormDialog handles add/edit</done>
</task>

<task type="auto">
  <name>Task 3: Create goals page with full integration</name>
  <files>src/app/(protected)/goals/page.tsx</files>
  <action>
Create /goals page that integrates all components:

1. Page header:
   - Title: "Goals & Actions"
   - Subtitle: "Manage your weekly commitments"
   - Add Goal button (opens GoalFormDialog in add mode)

2. Summary stats row:
   - Total goals count
   - Total hours/week committed
   - Active vs archived count

3. Filter/view options:
   - Toggle: Show archived (default: hidden)
   - Sort: By realm (default) / By hours / By deadline

4. Main content:
   - GoalsList component with all goals
   - Click goal card → opens GoalFormDialog in edit mode

5. Integration:
   - Use useGoals hook for data
   - Fetch realms from user's life_realms (API or from goals response)
   - Handle loading/error states
   - Toast notifications for success/error on CRUD operations

6. Responsive layout:
   - Similar to calendar page
   - Full width on mobile
   - Centered max-width container on desktop
  </action>
  <verify>
Page renders correctly:
- `npm run dev` and visit /goals
- Page loads without errors
- Add goal button opens dialog
  </verify>
  <done>Goals page displays user's goals with full CRUD functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /goals page renders goals grouped by realm
- [ ] Add goal opens dialog, submitting creates goal
- [ ] Edit goal opens pre-filled dialog, saving updates goal
- [ ] Delete goal removes from list
- [ ] Archive goal dims card, can be filtered
</verification>

<success_criteria>
- All tasks completed
- Goals page fully functional with CRUD
- Forms validate input correctly
- UI matches existing app aesthetic
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-goals-preferences/07-02-SUMMARY.md`
</output>
